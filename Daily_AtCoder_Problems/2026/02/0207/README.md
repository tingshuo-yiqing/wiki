## [D - Toggle Maze](https://atcoder.jp/contests/abc420/tasks/abc420_d)

考察分层图最短路，这里有两种状态：开和关。我们只需要开两层图即可。

1. 先记录终点和起点坐标

2. 初始化 $dist$ 数组，三维数组，第三维为状态

   * 当网格为 `o` 时 $0$ 可以通过 $1$ 不能通过
   * 当网格为 `x` 时 $1$ 可以通过 $0$ 不能通过

3.  当网格为 `?` 时进行状态改变 `ns = 1 - s`

4. 由于终点又多种状态，所以我们一达到终点就返回。

