算法原理：



关键三步：

1. 进入时：
2. 离开时：
3. 记录答案：



算法模板为：

```python
for i, x in enumerate(a):
    #! 进入
    while dq and a[dq[-1]] <= x:
        dq.pop()
    dq.append(i)

    #! 离开，这里是过期了必须离开
    if i - dq[0] >= k:  # [i-k+1, i]
        dq.popleft()
        0 1 2 3

    #! 记录答案
    if i >= k - 1:   # 形成了窗口，可以记录当前的最小值
        ans.append(a[dq[0]])
```

模板题：[P1886 单调队列](https://www.luogu.com.cn/problem/P1886) 

